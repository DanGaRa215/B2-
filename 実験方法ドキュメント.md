# お台場グルメ推薦システム - 実験・研究方法ドキュメント

## 📋 目次
1. [プロジェクト概要](#プロジェクト概要)
2. [データセット](#データセット)
3. [実験設計](#実験設計)
4. [実装技術](#実装技術)
5. [実験手順](#実験手順)
6. [評価指標](#評価指標)
7. [コード構成](#コード構成)
8. [実験結果の確認方法](#実験結果の確認方法)

---

## プロジェクト概要

### 研究目的
ユーザーの曖昧な食のイメージ（例：「あっさり」「静か」など）と利用シーンを考慮し、最適な飲食店を推薦するシステムにおいて、**どのデータが推薦精度に最も貢献するか**を定量的に検証する。

### 検証する仮説
- **仮説1**: カテゴリ情報だけでなく、レビューテキストを追加することで推薦精度が向上する
- **仮説2**: 星評価情報を追加することで、さらに推薦精度が向上する
- **仮説3**: 全情報（カテゴリ + レビュー + 星評価）の統合モデルが最高性能を発揮する

### 比較対象の3パターン

| パターン | 特徴量構成 | 目的 | 特徴量次元 |
|---------|----------|------|-----------|
| **P1** | カテゴリ + 星の数 | ベースライン性能測定 | 140次元 |
| **P2** | カテゴリ + レビュー | レビューの貢献度検証 | 639次元 |
| **P3** | カテゴリ + レビュー + 星の数 | 全情報統合モデル | 640次元 |

---

## データセット

### データソース
- **ファイル名**: `suku/odaiba_reviews_4.csv`
- **データ元**: 食べログ（Tabelog）のお台場エリア店舗レビュー

### データ規模
```
レビュー総数:  29,385件
店舗数:       404店舗
カテゴリ数:   139種類
対象エリア:   お台場、有明、東京ビッグサイト周辺
```

### データ構造
```csv
shop_name,shop_url,category,star_rating,review_text
YORIMICHI Odaiba,https://tabelog.com/...,台場駅,東京,海鮮,日本料理,居酒屋,3.33,"お台場のアクアシティの中にある..."
```

**各カラムの説明:**
- `shop_name`: 店舗名
- `shop_url`: 店舗の食べログURL
- `category`: カテゴリ（カンマ区切り、複数指定可能）
- `star_rating`: 星評価（1.0～5.0）
- `review_text`: レビュー本文

### データ前処理
1. **欠損値処理**: `star_rating`, `category`, `review_text`のいずれかが欠損している行を削除
2. **数値変換**: `star_rating`を数値型に変換（変換失敗は除外）
3. **カテゴリ分割**: カンマ区切りのカテゴリ文字列をリスト化
4. **店舗集約**: 同一店舗の複数レビューを1つに統合
   - 星評価: 平均値を算出
   - レビュー: 全レビューテキストを連結

---

## 実験設計

### 実験フロー
```
[データ読み込み]
    ↓
[データクレンジング]
    ↓
[店舗ごとに集約]
    ↓
[特徴量エンジニアリング]
    ├─ カテゴリエンコーディング (MultiLabelBinarizer)
    ├─ 星評価の正規化 (MinMaxScaler → L2正規化)
    └─ レビューのTF-IDF化 (Janome形態素解析)
    ↓
[パターン別特徴量行列作成]
    ├─ P1: カテゴリ + 星
    ├─ P2: カテゴリ + レビュー
    └─ P3: カテゴリ + レビュー + 星
    ↓
[テストシナリオ実行]
    └─ 各パターンでTop-5推薦を生成
    ↓
[評価・可視化]
    ├─ 類似度スコア統計
    ├─ 特徴量貢献度分析
    └─ グラフ出力
```

### テストシナリオ

実験では以下の3つのシナリオを使用：

#### シナリオ1: イタリアン、クリーミーなパスタ
```python
{
    'category': 'イタリアン',
    'review': 'クリーミーなパスタが食べたい。チーズたっぷりで濃厚な味わい',
    'rating': 3.5
}
```

#### シナリオ2: 和食、静かで落ち着いた雰囲気
```python
{
    'category': '日本料理',
    'review': '静かで落ち着いた雰囲気。上品で繊細な味付け。丁寧な接客',
    'rating': 4.0
}
```

#### シナリオ3: 海鮮、新鮮な刺身
```python
{
    'category': '海鮮',
    'review': '新鮮な刺身が食べたい。魚の甘みが感じられる。海の幸',
    'rating': 3.8
}
```

**シナリオ設計の意図:**
- 異なるジャンル（洋食、和食、海鮮）をカバー
- 具体的なイメージ語（「クリーミー」「静か」「新鮮」）を含む
- 中程度の星評価（3.5～4.0）を想定

---

## 実装技術

### 使用ライブラリ
```python
pandas              # データ処理
numpy               # 数値計算
scikit-learn        # 機械学習アルゴリズム
janome              # 日本語形態素解析
matplotlib          # 可視化
seaborn             # 高度な可視化
```

### コア技術スタック

#### 1. カテゴリエンコーディング
**手法**: `MultiLabelBinarizer`（scikit-learn）

**理由**:
- 1店舗が複数カテゴリを持つ（例: 「イタリアン, パスタ, ピザ」）
- One-Hotエンコーディングの多ラベル対応版

**処理例**:
```python
# 入力: [['イタリアン', 'パスタ'], ['日本料理', '寿司']]
# 出力: [[1, 0, 1, 0], [0, 1, 0, 1]]
#       (イタリアン, 日本料理, パスタ, 寿司)
```

#### 2. 星評価の正規化
**手法**: `MinMaxScaler` + L2正規化

**処理**:
```python
# Step1: MinMaxScalerで0～1の範囲に正規化
rating_features_raw = scaler.fit_transform(shop_grouped[['star_rating']])

# Step2: L2正規化（ベクトルのノルムを1に）
rating_features = normalize(rating_features_raw, norm='l2')
```

**重み付け（改善版v2）**:
```python
RATING_WEIGHT = 1.0
rating_features * RATING_WEIGHT
```

#### 3. レビューのベクトル化
**手法**: TF-IDF + 形態素解析

**処理フロー**:
```
[レビューテキスト]
    ↓ (形態素解析: Janome)
[トークン分割]
    ↓ (品詞フィルタ: 名詞・形容詞のみ)
[意味ある単語抽出]
    ↓ (ストップワード除去)
[クリーンなトークン列]
    ↓ (TF-IDF変換)
[500次元ベクトル]
```

**ストップワード定義**:
```python
STOP_WORDS = {
    'こと', 'もの', 'よう', 'ため', 'の', 'し', 'ん', 'さん',
    'これ', 'それ', 'あれ', 'この', 'その', 'あの',
    'ここ', 'そこ', 'あそこ', '今', '時', '感じ', '的',
    '場合', '時間', '場所', 'お店', '店', '店舗', '利用', '訪問', 'レビュー'
}
```

**TF-IDFパラメータ（改善版v2）**:
```python
TfidfVectorizer(
    max_features=500,        # 語彙数を3000→500に削減
    ngram_range=(1, 2)       # ユニグラム + バイグラム
)
```

**重み付け（改善版v2）**:
```python
REVIEW_WEIGHT = 1.0
CATEGORY_WEIGHT = 2.0  # カテゴリを重視
```

#### 4. 推薦アルゴリズム
**手法**: コサイン類似度 + カテゴリフィルタリング

**アルゴリズム**:
```python
def recommend_with_filter(user_vector, feature_matrix, shop_df,
                          category_filter=None, min_rating=None, top_k=5):
    # Step1: 全店舗との類似度計算
    similarities = cosine_similarity(user_vector, feature_matrix)[0]

    # Step2: カテゴリフィルタリング
    if category_filter:
        mask = shop_df['category_list'].apply(
            lambda cats: category_filter in cats
        )

    # Step3: 評価フィルタリング（オプション）
    if min_rating:
        mask &= (shop_df['star_rating'] >= min_rating)

    # Step4: フィルタ後の類似度でTop-K選定
    filtered_similarities = similarities[mask]
    top_indices = argsort(filtered_similarities)[-top_k:][::-1]

    return top_indices
```

**改善ポイント**:
- **基本版**: カテゴリ無視で全店舗から推薦 → イタリアン指定なのに日本料理店が推薦される問題
- **改善版**: カテゴリ一致店舗のみを推薦対象に → 適切な推薦を実現

---

## 実験手順

### 手順1: 環境構築
```bash
# 仮想環境作成（推奨）
python -m venv .venv
source .venv/bin/activate  # Windowsの場合: .venv\Scripts\activate

# 必要なライブラリインストール
pip install pandas numpy scikit-learn janome matplotlib seaborn
```

### 手順2: データ配置
```bash
# プロジェクトルートで実行
ls suku/odaiba_reviews_4.csv  # データファイルの存在確認
```

### 手順3: 実験実行

#### 方法A: Pythonスクリプトで実行
```bash
# 基本版実験
python run_experiment.py

# 改善版v2実験（正規化+バランス調整）
python run_experiment_improved_v2.py
```

#### 方法B: Jupyter Notebookで実行
```bash
# Jupyter起動
jupyter notebook

# ブラウザで以下を開く:
# - main.ipynb (基本版)
# - main_improved.ipynb (改善版)
```

### 手順4: 実行時の出力
実験実行中、以下のような進捗が表示されます：
```
================================================================================
改善版v2: お台場グルメ推薦システム - 特徴量正規化とバランス調整
================================================================================

[1/12] 初期化...
✅ 完了

[2/12] データ読み込み...
✅ 29,385行

[3/12] データクレンジング...
✅ 29,385行

[4/12] 店舗集約...
✅ 404店舗

[5/12] ストップワード定義...
✅ 完了

[6/12] 特徴量エンジニアリング（正規化版）...
   カテゴリ特徴量: (404, 139) (L2正規化済み)
   星評価特徴量: (404, 1) (L2正規化済み)
   レビュー特徴量: (404, 500) (L2正規化済み)
✅ 完了

[7/12] パターン別特徴量行列作成（重み付き）...
   P1: (404, 140) (カテゴリ重み×2.0)
   P2: (404, 639) (カテゴリ重み×2.0)
   P3: (404, 640) (カテゴリ重み×2.0)
✅ 完了

...
```

---

## 評価指標

### 主要指標: 平均類似度スコア
各パターンの推薦結果（Top-5）の類似度スコアの平均値を算出。

**計算式**:
```
平均類似度 = Σ(類似度スコア_i) / 5  (i=1〜5)
```

**解釈**:
- 高いほど、ユーザーの要求に近い店舗を推薦できている
- 0.0～1.0の範囲（コサイン類似度）

### 特徴量貢献度分析
各特徴量の追加効果を測定：

```python
レビュー貢献度 = P2の平均類似度 - P1の平均類似度
星評価貢献度 = P3の平均類似度 - P2の平均類似度
```

**解釈**:
- **正の値**: その特徴量の追加が精度向上に貢献
- **負の値**: その特徴量の追加が精度低下を招いた（ノイズとなった）

### 統計値
各パターンについて以下を算出：
- **平均類似度**: Top-5の平均
- **最大類似度**: 最も類似した店舗のスコア
- **最小類似度**: 5番目の店舗のスコア
- **平均評価**: Top-5の星評価の平均

---

## コード構成

### ファイル一覧
```
B2-store-recommend/
├── suku/
│   └── odaiba_reviews_4.csv              # データセット（29,385レビュー）
├── run_experiment.py                      # 基本版実験スクリプト
├── run_experiment_improved_v2.py          # 改善版v2実験スクリプト（推奨）
├── main.ipynb                             # 基本版Jupyterノートブック
├── main_improved.ipynb                    # 改善版Jupyterノートブック
├── experiment_summary.txt                 # 基本版実験結果サマリー
├── experiment_summary_v2.txt              # 改善版v2実験結果サマリー
├── pattern_comparison.png                 # パターン別比較グラフ（基本版）
├── pattern_comparison_v2.png              # パターン別比較グラフ（v2）
├── scenario_comparison.png                # シナリオ別比較グラフ（基本版）
├── scenario_comparison_v2.png             # シナリオ別比較グラフ（v2）
└── 総合結果レポート.md                    # 総合レポート（詳細分析）
```

### コードモジュール構成

#### `run_experiment_improved_v2.py`（推奨版）
```python
# 主要な関数・処理
├── preprocess_review_improved()          # レビュー前処理（形態素解析+ストップワード除去）
├── create_user_vector_p1()               # P1用ユーザーベクトル作成
├── create_user_vector_p2()               # P2用ユーザーベクトル作成
├── create_user_vector_p3()               # P3用ユーザーベクトル作成
├── recommend_with_filter()               # カテゴリフィルタリング対応推薦関数
└── main処理
    ├── データ読み込み・クレンジング
    ├── 特徴量エンジニアリング（L2正規化）
    ├── パターン別特徴量行列作成（重み付き）
    ├── テストシナリオ実行
    ├── 評価指標算出
    ├── 可視化・グラフ出力
    └── サマリーファイル出力
```

### 重要な設定パラメータ

#### 改善版v2のハイパーパラメータ
```python
# TF-IDF設定
max_features = 500              # 語彙数（3000から削減）
ngram_range = (1, 2)            # ユニグラム+バイグラム

# 特徴量重み
CATEGORY_WEIGHT = 2.0           # カテゴリの重要度を強化
REVIEW_WEIGHT = 1.0
RATING_WEIGHT = 1.0

# 推薦設定
top_k = 5                       # 推薦する店舗数
category_filter = True          # カテゴリフィルタリング有効化
```

---

## 実験結果の確認方法

### 1. テキストサマリーの確認
```bash
# 改善版v2の結果サマリー
cat experiment_summary_v2.txt
```

**出力内容**:
- データ概要（レビュー数、店舗数、カテゴリ数）
- パターン定義
- 類似度スコア統計（全シナリオ×全パターン）
- 特徴量貢献度分析
- 結論

### 2. グラフの確認
以下の画像ファイルを確認：

#### `pattern_comparison_v2.png`
- 3つのシナリオそれぞれで、P1/P2/P3の平均類似度を棒グラフで比較
- **読み取れること**: どのシナリオでも P1 > P3 > P2 の順で精度が高い

#### `scenario_comparison_v2.png`
- 横軸: 3つのテストシナリオ
- 縦軸: 平均類似度
- パターン別（P1/P2/P3）に色分けしてプロット
- **読み取れること**: シナリオ間での精度のばらつき、パターン間の一貫した傾向

### 3. 詳細分析レポートの確認
```bash
# 総合レポート（本実験の完全な分析）
cat 総合結果レポート.md
```

**含まれる内容**:
- 実験概要と目的
- 比較対象の3パターン詳細
- データ概要
- 実装技術の詳細
- 実験結果（数値+グラフ）
- 特徴量貢献度分析
- 主要な発見と考察
- 実用的な推奨事項
- 今後の改善方向

### 4. Jupyter Notebookでインタラクティブに確認
```bash
jupyter notebook main_improved.ipynb
```

**メリット**:
- コードをセル単位で実行可能
- データフレームの中身を直接確認
- グラフを即座に表示
- パラメータを変更して再実験が容易

---

## 実験の再現性

### 再現手順
```bash
# 1. リポジトリクローン
git clone <repository_url>
cd B2-store-recommend

# 2. 仮想環境構築
python -m venv .venv
source .venv/bin/activate

# 3. 依存関係インストール
pip install pandas numpy scikit-learn janome matplotlib seaborn

# 4. データ確認
ls suku/odaiba_reviews_4.csv

# 5. 実験実行
python run_experiment_improved_v2.py

# 6. 結果確認
cat experiment_summary_v2.txt
```

### 期待される実行時間
- データ読み込み: 約5秒
- 特徴量エンジニアリング: 約30秒
- 実験実行: 約10秒
- グラフ生成: 約5秒
- **合計**: 約1分

### 環境要件
- Python 3.7以上
- メモリ: 2GB以上推奨
- ディスク: 100MB以上の空き容量

---

## 基本版と改善版v2の違い

| 項目 | 基本版 | 改善版v2 |
|------|--------|---------|
| **TF-IDF次元数** | 3000 | 500（削減） |
| **正規化** | MinMaxScalerのみ | MinMaxScaler + L2正規化 |
| **カテゴリ重み** | ×1.0 | ×2.0（強化） |
| **カテゴリフィルタ** | なし | あり |
| **ストップワード** | なし | あり |
| **性能** | P1: 0.581 | P1: 0.597（向上） |

**改善版v2を使用する理由**:
1. **次元削減**により計算効率が向上
2. **L2正規化**により特徴量スケールの影響を排除
3. **カテゴリ重み付け**により、カテゴリの重要性を適切に反映
4. **カテゴリフィルタリング**により、不適切な推薦を防止
5. **ストップワード除去**により、ノイズを低減

---

## トラブルシューティング

### エラー: `FileNotFoundError: odaiba_reviews_4.csv`
**原因**: データファイルが見つからない

**解決策**:
```bash
# データファイルの存在確認
ls suku/odaiba_reviews_4.csv

# パスが異なる場合、スクリプト内のパスを修正
# run_experiment_improved_v2.py の33行目
CSV_FILE_PATH = '/path/to/your/odaiba_reviews_4.csv'
```

### エラー: `ModuleNotFoundError: No module named 'janome'`
**原因**: 必要なライブラリがインストールされていない

**解決策**:
```bash
pip install janome
```

### 警告: `UserWarning: matplotlib is currently using agg`
**原因**: グラフィカル環境がない（問題なし）

**説明**: この警告は無視して問題ありません。グラフは画像ファイルとして正常に保存されます。

### グラフが表示されない（Jupyter Notebook）
**原因**: matplotlib設定の問題

**解決策**:
```python
# ノートブックの先頭に追加
%matplotlib inline
```

---

## まとめ

この実験では、**お台場グルメ推薦システム**において3つのパターンを比較し、各特徴量の貢献度を定量的に評価しました。

### 実験の要点
1. **データ**: お台場エリア404店舗、29,385レビュー
2. **手法**: TF-IDF + コサイン類似度 + カテゴリフィルタリング
3. **評価**: 3つのテストシナリオで平均類似度を比較
4. **結果**: P1（カテゴリ+星評価）が最高性能（平均類似度: 0.597）

### 主要な発見
- **カテゴリ+星評価**が最も効果的（P1が最高性能）
- **レビュー情報の追加**は現状のTF-IDFでは精度低下を招く（-16%）
- **星評価の追加**は一定の効果あり（+9%）
- **カテゴリフィルタリング**は推薦の適切性に不可欠

### 今後の展開
1. 全国データへの拡張（404店舗→数千店舗）
2. BERT/Sentence-BERTによるレビュー処理の高度化
3. ハイブリッド推薦モデルの実装
4. ユーザー評価実験の実施

---

**ドキュメント作成日**: 2025年11月21日
**実験コード**: `run_experiment_improved_v2.py`
**データセット**: `suku/odaiba_reviews_4.csv`
**関連レポート**: `総合結果レポート.md`
