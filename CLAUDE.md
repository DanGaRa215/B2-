# 開発ルール(Claude への指示)

## 段階実装

- 複数ステージにまたがる実装は **1ステージずつ** 進める。
- **各ステージごとにブランチを切って** 作業する。
- ステージ完了時に報告し、**ユーザーが動作確認・承認してから** 次ステージへ進む。
- ステージの区切り以外では細かく質問して手を止めない。方針が決まっている範囲は
  自分で判断して進める。

## PR運用(マージ手前で止まる)

- ブランチ作成・コミット・プッシュ・**PR作成・タイトル/概要の記入までは Claude が行う**
  (`gh` CLI 使用可)。
- **マージするかどうかの最終判断はユーザーが行う。** Claude はマージ手前で必ず止まり、
  確認する。ユーザーが明示的に「マージして」と言った場合のみ `gh pr merge` を実行する。

## 破壊的操作の事前承認

- マイグレーション、データ削除、既存挙動を変える変更など不可逆な操作は、
  実行前に必ずユーザーの承認を取る。
- **認証情報・APIキー・`.env`・`token.json` には触れない**(上書き・削除しない)。
